<%= form_for(@excelsheet) do |f| %>
  <% if @excelsheet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@excelsheet.errors.count, "error") %> prohibited this excelsheet from being saved:</h2>

      <ul>
      <% @excelsheet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    
    <%= f.file_field :filename %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
