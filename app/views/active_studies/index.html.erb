
<div class="row narrow-container">

	<h3>Active Studies</h3>
	<hr>
	
	<table class="table table-striped">
	<thead>
	  <thead>
      <tr>
        <th>Date of Post</th>
        <th>Post Title</th>
        <th>Positive / Negative</th>
        <th>Price Targets</th>
        <% if current_user.present? and current_user.is_admin? %>
        	<th></th>
        <% end %>
      </tr>
    </thead>
	</thead>
	<tbody>
	  <% @active_studies.each do |study| %>
	  	<tr>
	  		<td><%= study.created_at.strftime("%B %d, %Y") %></td>
			<td><%= link_to study.title, study %></td>
			<td><%= study.typee %></td>
			<td><%= study.price_target %></td>
			<% if current_user.present? and current_user.is_admin? %>
				<td><span class="glyphicon glyphicon-scissors" aria-hidden="true"></span> &nbsp;&nbsp;<%= link_to 'Edit', edit_market_study_path(study) %></td>
			<% end %>
		</tr>
  	  <% end %>
	  
	</tbody>

	</table>

	<center><%= paginate @active_studies %></center>
</div>